@page "/"
@using Fordele.Blazor.Backend.Services;

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

Es werden aktuell <a href="/view-all">@UploadedFileService.GetUploadedFiles().Count() Dateien</a> verteilt.

@code {
    [Inject]
    public UploadedFileService UploadedFileService { get; set; } = null!;
}
